diff --git a/src/unifolm_vla/rlds_dataloader/datasets/rlds/oxe/mixtures.py b/src/unifolm_vla/rlds_dataloader/datasets/rlds/oxe/mixtures.py
--- a/src/unifolm_vla/rlds_dataloader/datasets/rlds/oxe/mixtures.py
+++ b/src/unifolm_vla/rlds_dataloader/datasets/rlds/oxe/mixtures.py
@@ -8,1 +8,13 @@
+import math
+import os
 from typing import Dict, List, Tuple
+
+
+def _g1_plush_real_weight() -> float:
+    """Read a bounded real-data sampling weight for controlled campaigns."""
+    value = float(os.environ.get("G1_PLUSH_REAL_WEIGHT", "3.0"))
+    if not math.isfinite(value) or not 1.0 <= value <= 12.0:
+        raise ValueError(
+            "G1_PLUSH_REAL_WEIGHT must be finite and between 1.0 and 12.0"
+        )
+    return value
@@ -14,1 +26,1 @@
-    "g1_plush_touch_mixed": [("g1_plush_touch_sim", 1.0), ("g1_plush_touch_real", 3.0)],
+    "g1_plush_touch_mixed": [("g1_plush_touch_sim", 1.0), ("g1_plush_touch_real", _g1_plush_real_weight())],
