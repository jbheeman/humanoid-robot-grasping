# G1 right-arm simulation baseline

No candidate has been promoted yet. Run the smoke test, then the 48–72 hour
sweep. Generated JSON files in this directory remain explicitly marked
`real_robot_validated: false` until guarded encoder telemetry is reviewed.
